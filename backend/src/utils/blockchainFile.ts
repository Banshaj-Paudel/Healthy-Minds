import axios from "axios";
import { API_URL } from "../config";

export const getB64Bytes = async (fileName: string): Promise<string> => {
  return "bytes";
};

export const uploadB64Bytes = async (b64Bytes: string): Promise<string> => {
  return "filename.jpg";
};
